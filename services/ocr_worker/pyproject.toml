[project]
name = "ocr_worker"
version = "0.1.0"
description = "An OCR worker service"
dependencies = [
    "SQLAlchemy",
    "PyMySQL",
    "python-dotenv",
    "boto3",
    "httpx",
]


[tool.setuptools]
packages = ["ocr_worker"]
